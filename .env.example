# Copy this file to .env and fill in real values.
# Used by docker-compose.yml for the db and backend services.
# .env is gitignored — never commit the real one.

DB_USER=writeflow_user
DB_PASSWORD=CHANGE_ME_placeholder_password
DB_NAME=writeflow_db
